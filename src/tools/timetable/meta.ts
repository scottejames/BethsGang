import type { ToolMeta } from '../types';

export const meta: ToolMeta = {
  id: 'timetable',
  name: 'Timetable',
  tagline: 'Lay out your week once — get a heads-up before each lesson',
  icon: '🗓️',
  category: 'study',
};
