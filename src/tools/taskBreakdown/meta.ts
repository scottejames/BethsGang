import type { ToolMeta } from '../types';

export const meta: ToolMeta = {
  id: 'task-breakdown',
  name: 'Task Breakdown',
  tagline: 'Turn an overwhelming task into small, startable steps',
  icon: '🧩',
};
